<Page
    x:Class="UpKeepProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UpKeepProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:app="using:UpKeepProject.Viewmodel.App"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <app:MainPageViewModel/>
    </Page.DataContext>

    <NavigationView SelectedItem ="{Binding SelectedMenuItem, Mode=TwoWay}">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Overview" Tag="NEED TO BE MADE"/>
            <NavigationViewItemSeparator/>
            <NavigationViewItem Content="Personale" Tag="OpenPersonaleView"/>
            <NavigationViewItem Content="Kunde" Tag="OpenKundeView"/>
        </NavigationView.MenuItems>
        <Frame x:Name="AppFrame"/>
    </NavigationView>
</Page>
